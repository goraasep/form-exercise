import { FC } from "react";
import Header from "../components/Header";
import Datatable from "../components/Datatable";
const Home: FC = () => {
  return (
    <div>
      <Header />
      <Datatable />
    </div>
  );
};
export default Home;
